// CloudTour AVP Configuration — copy this to Config.xcconfig and fill in real values
// Note: xcconfig treats // as comments so we store the host only (without https://)

SUPABASE_HOST = your-project-ref.supabase.co
SUPABASE_ANON_KEY = your-anon-key-here
API_BASE_URL = https://api.cloudtour.io
